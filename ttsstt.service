[Unit]
Description=Gunicorn instance for AI Flask app
After=network.target

[Service]
User=pedrolucas
Group=sudo
WorkingDirectory=/home/pedrolucas/AIXY-SERVER
ExecStart=/usr/local/bin/gunicorn -w 29 -b 0.0.0.0:PORT app:app

[Install]
WantedBy=multi-user.target
